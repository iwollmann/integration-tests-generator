const response = await request(app)
            .get('<%= request.name %>', { headers: 
            <%= request.headers.map(x=> '{ '+  x.name + ':' + x.value + '}') %> 
            })
            .send()
            .expect(200);